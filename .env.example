# Familiar Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# =============================================================================
# Git Provider Credentials
# =============================================================================

# GitHub Personal Access Token
# Required scopes: repo, write:discussion
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# GitHub Webhook Secret
# Generate a secure random string and use the same value when configuring the webhook
GITHUB_WEBHOOK_SECRET=your-secure-webhook-secret

# GitLab Personal Access Token
# Required scopes: api, read_repository, write_repository
# Create at: https://gitlab.com/-/profile/personal_access_tokens
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# GitLab Webhook Secret
# Generate a secure random string and use the same value when configuring the webhook
GITLAB_WEBHOOK_SECRET=your-secure-webhook-secret

# =============================================================================
# Paths (Required for Docker deployment)
# =============================================================================

# Absolute path on the HOST machine where git repos will be cached
# This is mounted into the Familiar container at /cache and also used for agent container bind mounts
# Example: /home/youruser/familiar-cache
REPO_CACHE_DIR=/path/to/your/cache/directory

# Directory for agent session logs (inside container)
# For Docker deployment, use /var/log/familiar (matches the named volume)
# For local development, use an absolute path like /home/youruser/familiar-logs
LOG_DIR=/var/log/familiar

# Absolute HOST path to your Claude CLI auth directory
# This is mounted into agent containers so they can authenticate
# Usually ~/.claude on the host machine
CLAUDE_AUTH_DIR=/home/youruser/.claude

# Docker image for agent containers
# Build with: docker build -t familiar-agent:latest -f docker/agent/Dockerfile .
AGENT_IMAGE=familiar-agent:latest

# =============================================================================
# Intent Parsing (API-based)
# =============================================================================

# Anthropic API Key (for intent parsing, not for agents)
# Agents use your Claude Code Max subscription via the CLI
# Create at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Optional: Test Configuration
# =============================================================================

# For running E2E tests against real repositories
# TEST_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# TEST_GITHUB_REPO=youruser/familiar-test
# TEST_GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
# TEST_GITLAB_REPO=youruser/familiar-test
